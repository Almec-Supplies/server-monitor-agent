# Agent Configuration
API_URL=http://localhost:5000
API_KEY=your-api-key-here
SERVER_NAME=localhost
INTERVAL_SECONDS=30

# Process Monitoring (comma-separated list of process names to monitor)
MONITORED_PROCESSES=nginx,postgresql,node
